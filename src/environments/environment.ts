// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyArFtSOsqmL0k31tCgy47dPKtffXTasr1o',
    authDomain: 'goalcoach-a4187.firebaseapp.com',
    databaseURL: 'https://goalcoach-a4187.firebaseio.com',
    projectId: 'goalcoach-a4187',
    storageBucket: 'goalcoach-a4187.appspot.com',
    messagingSenderId: '845378519150',
    appId: '1:845378519150:web:0272a1fa1b132622'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
